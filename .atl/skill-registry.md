# Skill Registry: centinela

> Auto-generated by sdd-init. Project-level skills preferred over user-level.

## Project-Level Skills

_None detected in project root._

## User-Level Skills (Selected)

| Skill | Trigger / Description | Compact Rules |
|-------|----------------------|---------------|
| `python-pro` | Python 3.12+, async, performance, production-ready practices. | Use modern Python features; prefer `pathlib` over `os.path`; use `uv`/ruff where available; strict typing with pydantic. |
| `python-testing-patterns` | pytest, fixtures, mocking, TDD. | Use pytest fixtures for shared state; mock external HTTP with respx; parametrize edge cases; keep tests deterministic. |
| `async-python-patterns` | asyncio, concurrent programming, async/await. | Use `async/await` for I/O-bound work; prefer `asyncio.gather` over sequential awaits; manage client lifecycle with context managers. |
| `007` | Security audit, hardening, threat modeling, code review. | Apply STRIDE/PASTA for threat modeling; review for injection, auth, and secrets leaks; validate all external inputs. |
| `backend-security-coder` | Secure backend coding: input validation, auth, API security. | Validate all inputs with pydantic; never trust external API responses; sanitize before logging; use least-privilege API keys. |
| `docker-expert` | Containerization, multi-stage builds, compose, security. | Use slim base images; pin versions; avoid running as root; multi-stage for reportlab/phonenumbers native deps if needed. |
| `bash-pro` / `bash-scripting` | Defensive shell scripting for automation. | Use `set -euo pipefail`; quote variables; validate inputs; prefer Python for complex logic. |
| `linux-shell-scripting` | Linux automation, system tasks. | Use systemd units for CLI services; package as `.deb`/`.rpm` for distro integration; respect FHS paths. |
| `code-review-checklist` | Thorough code reviews: functionality, security, performance. | Check for missing tests on new plugins; verify respx mocks cover error paths; ensure db migrations are backward-compatible. |
| `clean-code` | Clean Code principles: naming, functions, comments. | Functions should do one thing; avoid side effects in enrichment plugins; meaningful names over comments. |

## Convention Files

| File | Path | Status |
|------|------|--------|
| CLAUDE.md | `C:\Users\usuario\Documents\proyectos IA aplicada\centinela\CLAUDE.md` | Active |

## Notes

- No `.cursorrules`, `AGENTS.md`, `GEMINI.md`, or `copilot-instructions.md` found.
- Skills skipped: `sdd-*`, `_shared`, `skill-registry` (per scan rules).
- Relevant domain skills for future integration: `mcp-builder` (for exposing plugins as MCP tools), `api-design-principles` (if adding REST API).
